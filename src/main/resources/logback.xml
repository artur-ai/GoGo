<configuration>
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>log/gogo-debug.log</file>

        <encoder>
            <pattern>%d{yyyy-MM-dd} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>log/gogo-%d{yyyy-MM-dd}.log.%i</fileNamePattern>
            <maxFileSize>5mb</maxFileSize>
        </rollingPolicy>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>

        <encoder>
            <pattern>%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="emailAppender" class="ch.qos.logback.classic.net.SMTPAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
        </filter>

        <smtpHost>${SMTP_HOST}</smtpHost>
        <smtpPort>${SMTP_PORT}</smtpPort>
        <STARTTLS>true</STARTTLS>
        <username>${SMTP_USERNAME}</username>
        <password>${SMTP_PASSWORD}</password>
        <to>dkfedko@yahoo.com</to>
        <!--        <to>mauboroda.arturtu@gmail.com</to>-->
        <!--        <to>valentynabondarenkoo@gmail.com</to>-->
        <from>dkexhib@gmail.com</from>
        <subject>GOGO ERROR %logger{20} - %msg</subject>

        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{35} - %msg%n</pattern>
        </layout>
    </appender>

    <appender name="asyncEmailAppender" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="emailAppender"/>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="file" />
        <appender-ref ref="console" />
        <appender-ref ref="asyncEmailAppender"/>
    </root>
</configuration>